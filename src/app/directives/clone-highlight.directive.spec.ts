import { CloneHighlightDirective } from './clone-highlight.directive';

describe('CloneHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CloneHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
